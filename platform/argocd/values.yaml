global:
  domain: ""

configs:
  params:
    server.insecure: true

server:
  replicas: 2
  service:
    type: ClusterIP
  metrics:
    enabled: true
    serviceMonitor:
      enabled: false

controller:
  replicas: 1
  metrics:
    enabled: true
    serviceMonitor:
      enabled: false

repoServer:
  replicas: 2
  metrics:
    enabled: true
    serviceMonitor:
      enabled: false

applicationSet:
  replicas: 2
  metrics:
    enabled: true
    serviceMonitor:
      enabled: false

notifications:
  enabled: true
  metrics:
    enabled: true
    serviceMonitor:
      enabled: false

dex:
  enabled: true

redis:
  metrics:
    enabled: true
